type ItemType = {
  id: string;
  name: string;
  data?: {
    color?: string;
    capacity?: string;
    price?: string;
    generation?: string;
    year?: string;
  };
};

export type { ItemType };
